1) Implemente uma rotina computacional que leia o arquivo de entrada denominado “dados.txt”,
 o qual é constituído por duas colunas (os valores numéricos estão separados por espaço).
  
  
sendo que o espaçamento entre valores consecutivos de x é sempre igual ao mesmo Δx (uma constante), isto é, Δx = xi+1 - xi. 
Ou seja, o arquivo “dados.txt” contém n amostras de uma função f(x) cujos valores de x pertencem ao intervalo [x1, xn]. 
Após isso, o programa deve gerar o arquivo de saída “derivada.txt”. 
O valor de deve ser obtido por diferença avançada, o valor de deve ser obtido por diferença atrasada e os demais valores devem ser obtidos por diferença centrada.

 
2) Implemente uma rotina computacional que leia o arquivo de entrada denominado “dados.txt”, 
o qual é constituído por duas colunas (os valores numéricos estão separados por espaço).
  
  
sendo que o espaçamento entre valores consecutivos de x é sempre igual ao mesmo Δx (uma constante), 
isto é, Δx = xi+1 - xi. Ou seja, o arquivo “dados.txt” contém n amostras de uma função f(x) 
cujos valores de x pertencem ao intervalo [x1, xn]. Após isso, o programa deve gerar o arquivo de saída “integral.txt”.
  
  
  
O valor de é dado pela aproximação da integral utilizando o método dos retângulos (utilizando n-1 subintervalos de tamanho Δx). 
Bom trabalho a todos(as)!
